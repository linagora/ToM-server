image: node:lts-slim

stages:
  - build
  - test

npm:
  tags:
    - docker
  stage: build
  script:
    - npm install
    - npm run build
  cache:
    paths:
      - node_modules/
  artifacts:
    expire_in: 1 days
    when: on_success
    paths:
      - node_modules/
      - packages/*/dist/

test:
  tags:
    - docker
  stage: test
  dependencies:
    - npm
  script:
    - npm test
