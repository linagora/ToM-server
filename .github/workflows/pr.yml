name: pr

on:
  pull_request:
    branches:
      - dev
      - hotfixes
      - master
  merge_group:
  workflow_dispatch:

jobs:
  test:
    name: Build And Test
    uses: ./.github/workflows/build-and-test.yml
    secrets: inherit
